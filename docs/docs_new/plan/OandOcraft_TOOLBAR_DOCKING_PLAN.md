ge.tsx` - `BulkActionToolbar`): 
  - Implementation: Added `data-fixed-toolbar="roster-bulk"` and `data-fixed-toolbar-reason="Roster bulk toolbar is fixed inside the roster table because it is tied to the selected row set and table scroll context, not to the canvas."`
  - Status: Completed.

## Next Steps
- Verify the status of the `FloorSwitcher` component. If it is part of a pending phase, this task will remain open until the component is built.
